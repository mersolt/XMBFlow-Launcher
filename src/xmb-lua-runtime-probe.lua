-- XMBFlow Lua runtime probe.
-- Deliberately contains no graphics, storage, launcher, or system operations.
-- A successful launch exits at
-- once, proving that the Lua runtime can start independently of the UI.
System.exit()
